:css
  a#link_#{dom_id(page_element)} {
     color: #{page_element.css_color_spec};
     #{page_element.css_font_spec}
  }
.link_element{ }
  %a{ :href => page_element.url, :target => page_element.link_target, :id => "link_#{dom_id(page_element)}" }<
    = preserve(h(page_element.title).nbsp_leading_whitespace)
